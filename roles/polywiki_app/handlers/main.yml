---
# =============================================================================
# Handlers pour le rôle polywiki_app
# =============================================================================

- name: restart tomcat
  ansible.builtin.systemd:
    name: "{{ polywiki_tomcat_service_name }}"
    state: restarted
    daemon_reload: true

