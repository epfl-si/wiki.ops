---
# =============================================================================
# Handlers pour le rôle polywiki_apache
# =============================================================================

- name: reload apache
  ansible.builtin.service:
    name: "{{ polywiki_apache_service_name }}"
    state: reloaded

- name: restart apache
  ansible.builtin.service:
    name: "{{ polywiki_apache_service_name }}"
    state: restarted

